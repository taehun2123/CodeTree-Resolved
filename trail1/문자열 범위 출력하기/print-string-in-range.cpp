#include <bits/stdc++.h>
using namespace std;

int main() {
    cin.tie(0)->sync_with_stdio(0);
    string str;
    getline(cin, str);
    for(int i = 2; i<10; i++){
        cout << str[i];
    }
    return 0;
}